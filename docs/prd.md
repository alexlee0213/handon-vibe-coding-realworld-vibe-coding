# PRD: RealWorld Conduit 애플리케이션

> **Version**: 1.0
> **Last Updated**: 2025-01-30
> **Status**: Draft

## 1. 개요

### 1.1 프로젝트 배경

본 프로젝트는 RealWorld 스펙을 기반으로 한 풀스택 소셜 블로깅 플랫폼 "Conduit"를 Agentic Coding(바이브 코딩) 방식으로 구현합니다. Armin Ronacher의 "Agentic Coding" 원칙을 적용하여 AI 에이전트와의 협업에 최적화된 코드베이스를 구축합니다.

### 1.2 목표

1. RealWorld API 스펙 100% 준수
2. Agentic Coding에 최적화된 코드 구조
3. 프로덕션 수준의 품질 및 배포 자동화
4. 확장 가능하고 유지보수가 용이한 아키텍처

### 1.3 범위

- **In Scope**: 인증, 아티클 CRUD, 댓글, 태그, 프로필, 팔로우, 피드
- **Out of Scope**: 실시간 알림, 이메일 발송, 소셜 로그인, 결제

---

## 2. 기술 스택

### 2.1 Backend

| 구성요소 | 기술 | 버전 | 선정 근거 |
|----------|------|------|-----------|
| 언어 | Go | 1.21+ | 명시적 컨텍스트, 빠른 컴파일, LLM 친화적 구조 |
| HTTP 서버 | 표준 net/http | stdlib | 외부 의존성 최소화, Agentic Coding 원칙 준수 |
| 데이터베이스 (개발) | SQLite | 3.x | 설정 불필요, 빠른 개발 시작 |
| 데이터베이스 (운영) | PostgreSQL | 16 | 안정성, ACID, 확장성 |
| 데이터 접근 | Pure SQL | - | ORM 사용 안함, Plain SQL로 명시적 쿼리 |
| 마이그레이션 | golang-migrate | v4 | 버전 관리, 롤백 지원 |
| 인증 | golang-jwt | v5 | JWT 표준 구현 |
| 설정 관리 | envconfig | latest | 환경변수 기반 설정 |
| 로깅 | slog | stdlib | 구조화된 로깅, 표준 라이브러리 |
| 검증 | Go 표준 검증 | stdlib | 외부 의존성 최소화 |
| 테스트 | Go 표준 테스트 + testify | latest | 표준 테스트 + 가독성 향상 |

### 2.2 Frontend

| 구성요소 | 기술 | 버전 | 선정 근거 |
|----------|------|------|-----------|
| 프레임워크 | React | 18.3+ | 안정적 생태계, Armin 권장 |
| 빌드 도구 | Vite | 5+ | 빠른 HMR, 간단한 설정 |
| 언어 | TypeScript | 5.3+ | 타입 안전성 |
| 라우팅 | TanStack Router | latest | 타입 안전, 코드 기반 라우트 |
| 서버 상태 | TanStack Query | v5 | 캐싱, 동기화, 에러 처리 |
| 클라이언트 상태 | Zustand | latest | 경량, 간단한 API, 보일러플레이트 최소화 |
| UI 라이브러리 | Mantine | v7 | 120+ 컴포넌트, 접근성 |
| 폼 관리 | Mantine Form | v7 | Mantine 네이티브 통합 |
| 폼 검증 | Zod | latest | 타입 안전한 스키마 검증 |
| 스타일링 | Mantine CSS-in-JS | v7 | Mantine 테마 시스템 + 커스텀 CSS |
| 아이콘 | Tabler Icons | latest | Mantine 기본 아이콘 세트 |
| 알림 | Mantine Notifications | v7 | Mantine 네이티브 알림 시스템 |
| HTTP 클라이언트 | ky | latest | 간결한 API, 인터셉터 |
| 테스트 | Vitest + React Testing Library | latest | Vite 네이티브, 빠른 실행 |

### 2.3 Infrastructure

| 구성요소 | 기술 | 선정 근거 |
|----------|------|-----------|
| IaC | AWS CDK (TypeScript) | 타입 안전한 인프라 정의 |
| 컨테이너 | Docker | 일관된 환경 |
| 컴퓨팅 | AWS ECS Fargate | 서버리스 컨테이너 |
| 데이터베이스 | AWS RDS PostgreSQL | 관리형 DB |
| CDN | AWS CloudFront | 글로벌 배포 |
| 정적 호스팅 | AWS S3 | 프론트엔드 호스팅 |
| CI/CD | GitHub Actions | GitHub 통합 |

### 2.4 기술 스택 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   React     │  │    Vite     │  │ TypeScript  │  │   TanStack Router   │ │
│  │   18.3+     │  │     5+      │  │    5.3+     │  │      (Routing)      │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  TanStack   │  │   Zustand   │  │  Mantine    │  │    Mantine Form     │ │
│  │   Query     │  │  (Client)   │  │     UI      │  │      + Zod          │ │
│  │  (Server)   │  │             │  │             │  │                     │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────────────┐  │
│  │   Tabler    │  │   Vitest    │  │  React Testing Library              │  │
│  │   Icons     │  │             │  │                                     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTP/REST
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              BACKEND                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │   Go 1.21+  │  │  net/http   │  │    slog     │  │    golang-jwt       │ │
│  │             │  │  (stdlib)   │  │  (logging)  │  │      (auth)         │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────────────┐  │
│  │  Pure SQL   │  │   testify   │  │  golang-migrate (migrations)        │  │
│  │  (no ORM)   │  │  (testing)  │  │                                     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ SQL
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             DATABASE                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────┐  ┌─────────────────────────────────────┐   │
│  │   SQLite (Development)      │  │   PostgreSQL (Production)           │   │
│  │   - Zero config             │  │   - AWS RDS                         │   │
│  │   - Fast iteration          │  │   - Scalable                        │   │
│  └─────────────────────────────┘  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Agentic Coding 원칙

Armin Ronacher의 권장사항을 기반으로 한 개발 원칙입니다.

### 3.1 코드 작성 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                    AGENTIC CODING PRINCIPLES                    │
├─────────────────────────────────────────────────────────────────┤
│  1. 명확하고 긴 함수명 > 짧고 모호한 함수명                        │
│  2. 합성(Composition) > 상속(Inheritance)                       │
│  3. Plain SQL + sqlc > ORM                                      │
│  4. 명시적 권한 체크 > 설정 파일 기반 권한                         │
│  5. 코드 생성 > 외부 의존성 추가                                   │
│  6. 구조화된 로깅 > printf 디버깅                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.1 함수 명명 규칙

```go
// ❌ 피해야 할 스타일
func GetUser(id int) (*User, error)

// ✅ 권장 스타일 - 명확하고 설명적인 이름
func GetUserByID(ctx context.Context, userID int64) (*User, error)
func GetUserByEmail(ctx context.Context, email string) (*User, error)
func ListArticlesByAuthorUsername(ctx context.Context, username string, limit, offset int) ([]Article, error)
```

#### 3.1.2 권한 체크 패턴

```go
// ❌ 설정 파일에 숨겨진 권한 체크
// config/permissions.yaml에서 관리

// ✅ 코드에서 명시적으로 확인
func (s *ArticleService) DeleteArticle(ctx context.Context, slug string, currentUserID int64) error {
    article, err := s.repo.GetArticleBySlug(ctx, slug)
    if err != nil {
        return err
    }

    // 권한 체크가 코드에 명시적으로 보임
    if article.AuthorID != currentUserID {
        return ErrForbidden
    }

    return s.repo.DeleteArticle(ctx, article.ID)
}
```

#### 3.1.3 에러 처리 패턴

```go
// 도메인 에러 정의 - 명확하고 추적 가능
var (
    ErrUserNotFound      = errors.New("user not found")
    ErrArticleNotFound   = errors.New("article not found")
    ErrInvalidCredentials = errors.New("invalid email or password")
    ErrEmailAlreadyTaken  = errors.New("email already taken")
    ErrUsernameAlreadyTaken = errors.New("username already taken")
    ErrForbidden         = errors.New("forbidden: insufficient permissions")
)
```

### 3.2 도구 설계 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                      TOOL DESIGN PRINCIPLES                     │
├─────────────────────────────────────────────────────────────────┤
│  1. 빠른 실행 - 3ms 응답 vs 5초 컴파일은 천지차이                  │
│  2. 명확한 에러 메시지 - 에이전트가 이해할 수 있는 출력             │
│  3. 오남용 방지 - LLM chaos monkey에 대한 방어                    │
│  4. 관찰 가능성 - 로그를 통한 디버깅                               │
│  5. 멱등성 - 여러 번 실행해도 동일한 결과                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 로깅 전략

```go
// 구조화된 로깅 - 에이전트가 파싱하기 쉬움
slog.Info("article created",
    "article_id", article.ID,
    "slug", article.Slug,
    "author_id", article.AuthorID,
    "tags", article.TagList,
)

slog.Error("failed to create article",
    "error", err,
    "user_id", userID,
    "title", req.Title,
)
```

### 3.4 Makefile 기반 워크플로우

```makefile
# 에이전트가 사용할 주요 명령어들
.PHONY: dev test lint build migrate generate deploy

# 개발 서버 실행 (pidfile로 중복 실행 방지)
dev:
	@./scripts/dev.sh

# 테스트 실행 (증분 테스트 지원)
test:
	cd backend && go test -v ./...
	cd frontend && npm run test

# 프론트엔드 테스트 (watch 모드)
test-watch:
	cd frontend && npm run test:watch

# 테스트 커버리지
test-coverage:
	cd backend && go test -v -coverprofile=coverage.out ./...
	cd frontend && npm run test:coverage

# 린트 검사
lint:
	cd backend && golangci-lint run
	cd frontend && npm run lint

# 프로덕션 빌드
build:
	cd backend && go build -o bin/server ./cmd/server
	cd frontend && npm run build

# DB 마이그레이션
migrate:
	cd backend && migrate -path db/migrations -database "$$DATABASE_URL" up

migrate-down:
	cd backend && migrate -path db/migrations -database "$$DATABASE_URL" down 1

# sqlc 코드 생성
generate:
	cd backend && sqlc generate

# 배포
deploy:
	cd infra && cdk deploy --all
```

---

## 4. 시스템 아키텍처

### 4.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                          INTERNET                                   │
└─────────────────────────────────┬───────────────────────────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │     AWS CloudFront        │
                    │     (CDN + HTTPS)         │
                    └─────────────┬─────────────┘
                                  │
            ┌─────────────────────┴─────────────────────┐
            │                                           │
┌───────────▼───────────┐               ┌───────────────▼───────────┐
│       S3 Bucket       │               │    Application Load       │
│   (Frontend SPA)      │               │       Balancer            │
│                       │               └───────────────┬───────────┘
│  - React App          │                               │
│  - Static Assets      │               ┌───────────────▼───────────┐
└───────────────────────┘               │     ECS Fargate           │
                                        │     (Backend API)         │
                                        │                           │
                                        │  ┌─────────────────────┐  │
                                        │  │   Go Application    │  │
                                        │  │   - Chi Router      │  │
                                        │  │   - JWT Auth        │  │
                                        │  │   - sqlc Queries    │  │
                                        │  └─────────────────────┘  │
                                        └───────────────┬───────────┘
                                                        │
                                        ┌───────────────▼───────────┐
                                        │      RDS PostgreSQL       │
                                        │      (Primary DB)         │
                                        └───────────────────────────┘
```

### 4.2 Backend 레이어 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                     HTTP Layer (net/http)                       │
│  - 표준 라이브러리 기반 라우팅                                     │
│  - 미들웨어 체인                                                  │
│  - 요청/응답 직렬화 (encoding/json)                               │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│                       Service Layer                             │
│  - 비즈니스 로직, 권한 체크, 트랜잭션 관리                         │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│                   Repository Layer (Pure SQL)                   │
│  - 데이터 접근, Plain SQL 쿼리 실행                               │
│  - database/sql 표준 라이브러리 사용                              │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│              SQLite (Dev) / PostgreSQL (Prod)                   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 Frontend 레이어 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                      Routes (TanStack Router)                   │
│  - 페이지 컴포넌트, 라우트 정의, 레이아웃                          │
│  - 타입 안전 라우팅, 코드 기반 라우트 정의                         │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│                    Features (기능별 모듈)                        │
│  - 컴포넌트, 훅, 타입 정의                                        │
│  - Mantine Form + Zod 폼 검증                                    │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│                   Shared Components (Mantine)                   │
│  - 공통 UI 컴포넌트, 레이아웃                                     │
│  - Tabler Icons 아이콘 시스템                                    │
└─────────────────────────────────┬───────────────────────────────┘
                                  │
┌─────────────────────────────────▼───────────────────────────────┐
│                        State Management                         │
│  ┌─────────────────────────┐  ┌─────────────────────────────┐   │
│  │   TanStack Query        │  │       Zustand               │   │
│  │   (Server State)        │  │   (Client State)            │   │
│  │   - API 캐싱            │  │   - UI 상태                 │   │
│  │   - 백그라운드 동기화    │  │   - 인증 토큰               │   │
│  │   - 낙관적 업데이트      │  │   - 사용자 설정             │   │
│  └─────────────────────────┘  └─────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 상태 관리 전략

#### 4.4.1 서버 상태 (TanStack Query)

서버에서 가져오는 모든 데이터는 TanStack Query로 관리합니다.

```typescript
// features/article/hooks.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { articleApi } from './api';

// 아티클 목록 조회
export function useArticles(params?: ArticleListParams) {
  return useQuery({
    queryKey: ['articles', params],
    queryFn: () => articleApi.list(params),
    staleTime: 1000 * 60, // 1분
  });
}

// 아티클 생성 (낙관적 업데이트)
export function useCreateArticle() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: articleApi.create,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['articles'] });
    },
  });
}
```

#### 4.4.2 클라이언트 상태 (Zustand)

UI 상태와 인증 정보는 Zustand로 관리합니다.

```typescript
// features/auth/store.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  token: string | null;
  user: User | null;
  setAuth: (token: string, user: User) => void;
  clearAuth: () => void;
  isAuthenticated: () => boolean;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      token: null,
      user: null,
      setAuth: (token, user) => set({ token, user }),
      clearAuth: () => set({ token: null, user: null }),
      isAuthenticated: () => get().token !== null,
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

#### 4.4.3 폼 상태 (Mantine Form + Zod)

폼 데이터와 검증은 Mantine Form과 Zod로 처리합니다.

```typescript
// features/auth/components/LoginForm.tsx
import { useForm, zodResolver } from '@mantine/form';
import { z } from 'zod';

const loginSchema = z.object({
  email: z.string().email('유효한 이메일을 입력하세요'),
  password: z.string().min(6, '비밀번호는 6자 이상이어야 합니다'),
});

type LoginFormValues = z.infer<typeof loginSchema>;

export function LoginForm() {
  const form = useForm<LoginFormValues>({
    initialValues: {
      email: '',
      password: '',
    },
    validate: zodResolver(loginSchema),
  });

  return (
    <form onSubmit={form.onSubmit(handleSubmit)}>
      <TextInput
        label="이메일"
        {...form.getInputProps('email')}
      />
      <PasswordInput
        label="비밀번호"
        {...form.getInputProps('password')}
      />
      <Button type="submit">로그인</Button>
    </form>
  );
}
```

---

## 5. 데이터 모델

### 5.1 ERD

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│     users       │       │    articles     │       │    comments     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │───┐   │ id (PK)         │───┐   │ id (PK)         │
│ username        │   │   │ slug (UNIQUE)   │   │   │ body            │
│ email (UNIQUE)  │   │   │ title           │   │   │ article_id (FK) │──┐
│ password_hash   │   │   │ description     │   │   │ author_id (FK)  │──┼─┐
│ bio             │   │   │ body            │   │   │ created_at      │  │ │
│ image           │   └──▶│ author_id (FK)  │   │   │ updated_at      │  │ │
│ created_at      │       │ created_at      │   │   └─────────────────┘  │ │
│ updated_at      │       │ updated_at      │   │                        │ │
└─────────────────┘       └─────────────────┘   │   ┌─────────────────┐  │ │
        │                         │             └──▶│    articles     │◀─┘ │
        │                         │                 └─────────────────┘    │
        │                         │                                        │
        │    ┌─────────────────┐  │   ┌─────────────────┐                 │
        │    │    follows      │  │   │ article_tags    │                 │
        │    ├─────────────────┤  │   ├─────────────────┤                 │
        │    │ follower_id(FK) │◀─┤   │ article_id (FK) │◀────────────────┘
        └───▶│ following_id(FK)│  │   │ tag_id (FK)     │
             │ created_at      │  │   └─────────────────┘
             └─────────────────┘  │           │
                                  │   ┌───────▼─────────┐
        ┌─────────────────┐       │   │     tags        │
        │   favorites     │       │   ├─────────────────┤
        ├─────────────────┤       │   │ id (PK)         │
        │ user_id (FK)    │◀──────┤   │ name (UNIQUE)   │
        │ article_id (FK) │◀──────┘   │ created_at      │
        │ created_at      │           └─────────────────┘
        └─────────────────┘
```

### 5.2 스키마 정의

```sql
-- users 테이블
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    email VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    bio TEXT DEFAULT '',
    image VARCHAR(512) DEFAULT '',
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- articles 테이블
CREATE TABLE articles (
    id BIGSERIAL PRIMARY KEY,
    slug VARCHAR(255) NOT NULL UNIQUE,
    title VARCHAR(255) NOT NULL,
    description TEXT NOT NULL,
    body TEXT NOT NULL,
    author_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- tags 테이블
CREATE TABLE tags (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- article_tags 테이블 (다대다 관계)
CREATE TABLE article_tags (
    article_id BIGINT NOT NULL REFERENCES articles(id) ON DELETE CASCADE,
    tag_id BIGINT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
    PRIMARY KEY (article_id, tag_id)
);

-- comments 테이블
CREATE TABLE comments (
    id BIGSERIAL PRIMARY KEY,
    body TEXT NOT NULL,
    article_id BIGINT NOT NULL REFERENCES articles(id) ON DELETE CASCADE,
    author_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- follows 테이블
CREATE TABLE follows (
    follower_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    following_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (follower_id, following_id),
    CHECK (follower_id != following_id)
);

-- favorites 테이블
CREATE TABLE favorites (
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    article_id BIGINT NOT NULL REFERENCES articles(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (user_id, article_id)
);

-- 인덱스
CREATE INDEX idx_articles_author_id ON articles(author_id);
CREATE INDEX idx_articles_created_at ON articles(created_at DESC);
CREATE INDEX idx_comments_article_id ON comments(article_id);
CREATE INDEX idx_follows_following_id ON follows(following_id);
CREATE INDEX idx_favorites_article_id ON favorites(article_id);
```

---

## 6. API 명세

### 6.1 인증 API

#### POST /api/users (회원가입)

```json
// Request
{
  "user": {
    "username": "johndoe",
    "email": "john@example.com",
    "password": "secret123"
  }
}

// Response 201 Created
{
  "user": {
    "email": "john@example.com",
    "token": "jwt.token.here",
    "username": "johndoe",
    "bio": "",
    "image": ""
  }
}

// Response 422 Unprocessable Entity
{
  "errors": {
    "email": ["has already been taken"],
    "username": ["has already been taken"]
  }
}
```

#### POST /api/users/login (로그인)

```json
// Request
{
  "user": {
    "email": "john@example.com",
    "password": "secret123"
  }
}

// Response 200 OK
{
  "user": {
    "email": "john@example.com",
    "token": "jwt.token.here",
    "username": "johndoe",
    "bio": "I work at statefarm",
    "image": "https://example.com/avatar.png"
  }
}

// Response 401 Unauthorized
{
  "errors": {
    "body": ["invalid email or password"]
  }
}
```

#### GET /api/user (현재 사용자 조회)

```
Authorization: Token jwt.token.here
```

```json
// Response 200 OK
{
  "user": {
    "email": "john@example.com",
    "token": "jwt.token.here",
    "username": "johndoe",
    "bio": "I work at statefarm",
    "image": "https://example.com/avatar.png"
  }
}
```

#### PUT /api/user (사용자 정보 수정)

```json
// Request
{
  "user": {
    "email": "john@example.com",
    "bio": "Updated bio",
    "image": "https://example.com/new-avatar.png"
  }
}

// Response 200 OK
{
  "user": {
    "email": "john@example.com",
    "token": "jwt.token.here",
    "username": "johndoe",
    "bio": "Updated bio",
    "image": "https://example.com/new-avatar.png"
  }
}
```

### 6.2 프로필 API

#### GET /api/profiles/:username

```json
// Response 200 OK
{
  "profile": {
    "username": "johndoe",
    "bio": "I work at statefarm",
    "image": "https://example.com/avatar.png",
    "following": false
  }
}
```

#### POST /api/profiles/:username/follow

```json
// Response 200 OK
{
  "profile": {
    "username": "johndoe",
    "bio": "I work at statefarm",
    "image": "https://example.com/avatar.png",
    "following": true
  }
}
```

### 6.3 아티클 API

#### GET /api/articles

```
Query Parameters:
- tag: string (필터링)
- author: string (필터링)
- favorited: string (필터링)
- limit: number (기본값: 20)
- offset: number (기본값: 0)
```

```json
// Response 200 OK
{
  "articles": [
    {
      "slug": "how-to-train-your-dragon",
      "title": "How to train your dragon",
      "description": "Ever wonder how?",
      "body": "It takes a Jacobian",
      "tagList": ["dragons", "training"],
      "createdAt": "2016-02-18T03:22:56.637Z",
      "updatedAt": "2016-02-18T03:48:35.824Z",
      "favorited": false,
      "favoritesCount": 0,
      "author": {
        "username": "johndoe",
        "bio": "I work at statefarm",
        "image": "https://example.com/avatar.png",
        "following": false
      }
    }
  ],
  "articlesCount": 1
}
```

#### GET /api/articles/feed

```json
// Response 200 OK (인증 필수)
{
  "articles": [...],
  "articlesCount": 10
}
```

#### POST /api/articles

```json
// Request
{
  "article": {
    "title": "How to train your dragon",
    "description": "Ever wonder how?",
    "body": "You have to believe",
    "tagList": ["reactjs", "angularjs", "dragons"]
  }
}

// Response 201 Created
{
  "article": {
    "slug": "how-to-train-your-dragon",
    "title": "How to train your dragon",
    "description": "Ever wonder how?",
    "body": "You have to believe",
    "tagList": ["reactjs", "angularjs", "dragons"],
    "createdAt": "2016-02-18T03:22:56.637Z",
    "updatedAt": "2016-02-18T03:22:56.637Z",
    "favorited": false,
    "favoritesCount": 0,
    "author": {
      "username": "johndoe",
      "bio": "I work at statefarm",
      "image": "https://example.com/avatar.png",
      "following": false
    }
  }
}
```

### 6.4 댓글 API

#### GET /api/articles/:slug/comments

```json
// Response 200 OK
{
  "comments": [
    {
      "id": 1,
      "createdAt": "2016-02-18T03:22:56.637Z",
      "updatedAt": "2016-02-18T03:22:56.637Z",
      "body": "It takes a Jacobian",
      "author": {
        "username": "johndoe",
        "bio": "I work at statefarm",
        "image": "https://example.com/avatar.png",
        "following": false
      }
    }
  ]
}
```

#### POST /api/articles/:slug/comments

```json
// Request
{
  "comment": {
    "body": "His name was my name too."
  }
}

// Response 201 Created
{
  "comment": {
    "id": 1,
    "createdAt": "2016-02-18T03:22:56.637Z",
    "updatedAt": "2016-02-18T03:22:56.637Z",
    "body": "His name was my name too.",
    "author": {
      "username": "johndoe",
      "bio": "I work at statefarm",
      "image": "https://example.com/avatar.png",
      "following": false
    }
  }
}
```

### 6.5 태그 API

#### GET /api/tags

```json
// Response 200 OK
{
  "tags": ["reactjs", "angularjs", "dragons", "training"]
}
```

### 6.6 에러 응답 형식

```json
// 유효성 검증 에러 (422)
{
  "errors": {
    "title": ["can't be blank"],
    "body": ["can't be blank"]
  }
}

// 인증 에러 (401)
{
  "errors": {
    "body": ["unauthorized"]
  }
}

// 권한 에러 (403)
{
  "errors": {
    "body": ["forbidden"]
  }
}

// 리소스 없음 (404)
{
  "errors": {
    "body": ["not found"]
  }
}
```

---

## 7. 프론트엔드 명세

### 7.1 라우트 구조

```typescript
// TanStack Router 기반 라우트 정의
const routeTree = rootRoute.addChildren([
  indexRoute,           // /
  loginRoute,           // /login
  registerRoute,        // /register
  settingsRoute,        // /settings (protected)
  editorRoute,          // /editor (protected)
  editorSlugRoute,      // /editor/:slug (protected)
  articleRoute,         // /article/:slug
  profileRoute.addChildren([
    profileIndexRoute,    // /profile/:username
    profileFavoritesRoute // /profile/:username/favorites
  ])
])
```

### 7.2 페이지별 컴포넌트 구성

#### 홈 페이지 (/)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Header                                  │
├───────────────────────────────────────┬─────────────────────────┤
│                                       │                         │
│  ┌─────────────────────────────────┐  │  ┌───────────────────┐  │
│  │ Feed Tabs                       │  │  │ Popular Tags      │  │
│  │ [Your Feed] [Global Feed] [#tag]│  │  │ ┌───┐ ┌───┐ ┌───┐ │  │
│  └─────────────────────────────────┘  │  │ │tag│ │tag│ │tag│ │  │
│                                       │  │ └───┘ └───┘ └───┘ │  │
│  ┌─────────────────────────────────┐  │  └───────────────────┘  │
│  │ Article Preview                 │  │                         │
│  │ ┌────┐ Author · Date           │  │                         │
│  │ │ Av │ Title                    │  │                         │
│  │ └────┘ Description              │  │                         │
│  │        [Read more...] ♡ Count   │  │                         │
│  └─────────────────────────────────┘  │                         │
│                                       │                         │
│  ┌─────────────────────────────────┐  │                         │
│  │ Pagination                      │  │                         │
│  │ [1] [2] [3] ... [10]           │  │                         │
│  └─────────────────────────────────┘  │                         │
│                                       │                         │
└───────────────────────────────────────┴─────────────────────────┘
```

#### 아티클 상세 페이지 (/article/:slug)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Header                                  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    Article Banner                           ││
│  │  Title                                                      ││
│  │  ┌────┐ Author · Date  [Edit] [Delete] or [Follow] [Fav]   ││
│  │  │ Av │                                                     ││
│  │  └────┘                                                     ││
│  └─────────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Article Body (Markdown Rendered)                               │
│                                                                 │
│  Tag List: [tag1] [tag2] [tag3]                                │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Comments Section                                            ││
│  │                                                             ││
│  │ ┌─────────────────────────────────────────────────────────┐ ││
│  │ │ Comment Form (logged in) or Sign in link                │ ││
│  │ └─────────────────────────────────────────────────────────┘ ││
│  │                                                             ││
│  │ ┌─────────────────────────────────────────────────────────┐ ││
│  │ │ Comment Card                                            │ ││
│  │ │ Comment body...                                         │ ││
│  │ │ ┌────┐ Author · Date                     [🗑 Delete]    │ ││
│  │ │ │ Av │                                                  │ ││
│  │ │ └────┘                                                  │ ││
│  │ └─────────────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 Mantine 컴포넌트 매핑

| 기능 | Mantine 컴포넌트 | 추가 라이브러리 |
|------|------------------|-----------------|
| 전체 레이아웃 | `AppShell`, `Container` | - |
| 네비게이션 | `Group`, `Anchor`, `Button` | - |
| 아티클 카드 | `Card`, `Group`, `Avatar`, `Text`, `Badge` | - |
| 아티클 에디터 | `TextInput`, `Textarea`, `TagsInput`, `Button` | `@mantine/form` |
| 댓글 | `Paper`, `Stack`, `Avatar`, `Text`, `ActionIcon` | - |
| 폼 (로그인/가입) | `TextInput`, `PasswordInput`, `Button`, `Stack` | `@mantine/form` + `zod` |
| 태그 | `Badge`, `Group` | - |
| 페이지네이션 | `Pagination` | - |
| 피드 탭 | `Tabs` | - |
| 알림 | `notifications.show()` | `@mantine/notifications` |
| 로딩 상태 | `Skeleton`, `Loader`, `LoadingOverlay` | - |
| 에러 표시 | `Alert` | - |
| 아이콘 | - | `@tabler/icons-react` |

### 7.4 Tabler Icons 사용 예시

```typescript
import {
  IconHeart,
  IconHeartFilled,
  IconUser,
  IconSettings,
  IconEdit,
  IconTrash,
  IconPlus,
} from '@tabler/icons-react';

// 좋아요 버튼
<ActionIcon variant="subtle" color={favorited ? 'red' : 'gray'}>
  {favorited ? <IconHeartFilled size={18} /> : <IconHeart size={18} />}
</ActionIcon>

// 설정 링크
<Anchor component={Link} to="/settings">
  <IconSettings size={16} /> Settings
</Anchor>
```

### 7.5 Mantine Notifications 사용

```typescript
import { notifications } from '@mantine/notifications';
import { IconCheck, IconX } from '@tabler/icons-react';

// 성공 알림
notifications.show({
  title: '성공',
  message: '아티클이 생성되었습니다',
  color: 'green',
  icon: <IconCheck size={18} />,
});

// 에러 알림
notifications.show({
  title: '오류',
  message: '아티클 생성에 실패했습니다',
  color: 'red',
  icon: <IconX size={18} />,
});
```

### 7.6 테마 설정

```typescript
import { createTheme, MantineProvider } from '@mantine/core';
import { Notifications } from '@mantine/notifications';

const theme = createTheme({
  primaryColor: 'green',
  fontFamily: 'Source Sans Pro, sans-serif',
  headings: {
    fontFamily: 'Titillium Web, sans-serif',
    fontWeight: '700',
  },
  colors: {
    // RealWorld 브랜드 색상
    brand: [
      '#e6f7e6',
      '#c3ebc3',
      '#9fdf9f',
      '#7bd37b',
      '#5cb85c',  // Primary
      '#4cae4c',
      '#3d8b3d',
      '#2d682d',
      '#1e451e',
      '#0f220f',
    ],
  },
  components: {
    Button: {
      defaultProps: {
        radius: 'sm',
      },
    },
    Card: {
      defaultProps: {
        radius: 'sm',
        withBorder: true,
      },
    },
  },
});

// App.tsx에서 사용
function App() {
  return (
    <MantineProvider theme={theme}>
      <Notifications position="top-right" />
      {/* ... */}
    </MantineProvider>
  );
}
```

---

## 8. 프로젝트 구조

### 8.1 Backend 구조

```
backend/
├── cmd/
│   └── server/
│       └── main.go              # 진입점
├── internal/
│   ├── api/
│   │   ├── handler/
│   │   │   ├── user.go          # 사용자 핸들러
│   │   │   ├── article.go       # 아티클 핸들러
│   │   │   ├── comment.go       # 댓글 핸들러
│   │   │   ├── profile.go       # 프로필 핸들러
│   │   │   └── tag.go           # 태그 핸들러
│   │   ├── middleware/
│   │   │   ├── auth.go          # JWT 인증
│   │   │   ├── logging.go       # 요청 로깅
│   │   │   └── cors.go          # CORS 설정
│   │   ├── request/
│   │   │   └── request.go       # 요청 DTO
│   │   ├── response/
│   │   │   └── response.go      # 응답 DTO
│   │   └── router.go            # 라우터 설정
│   ├── config/
│   │   └── config.go            # 환경 설정
│   ├── domain/
│   │   ├── user.go              # 사용자 도메인
│   │   ├── article.go           # 아티클 도메인
│   │   ├── comment.go           # 댓글 도메인
│   │   └── errors.go            # 도메인 에러
│   ├── repository/
│   │   ├── user.go              # 사용자 저장소
│   │   ├── article.go           # 아티클 저장소
│   │   ├── comment.go           # 댓글 저장소
│   │   └── tag.go               # 태그 저장소
│   └── service/
│       ├── user.go              # 사용자 서비스
│       ├── article.go           # 아티클 서비스
│       ├── comment.go           # 댓글 서비스
│       └── auth.go              # 인증 서비스
├── db/
│   ├── migrations/
│   │   ├── 000001_create_users_table.up.sql
│   │   ├── 000001_create_users_table.down.sql
│   │   └── ...
│   ├── queries/
│   │   ├── user.sql             # 사용자 쿼리
│   │   ├── article.sql          # 아티클 쿼리
│   │   ├── comment.sql          # 댓글 쿼리
│   │   └── tag.sql              # 태그 쿼리
│   └── sqlc.yaml                # sqlc 설정
├── scripts/
│   └── dev.sh                   # 개발 서버 스크립트
├── Dockerfile
├── Makefile
├── go.mod
└── go.sum
```

### 8.2 Frontend 구조

```
frontend/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Layout.tsx
│   │   ├── article/
│   │   │   ├── ArticleCard.tsx
│   │   │   ├── ArticleMeta.tsx
│   │   │   └── ArticleList.tsx
│   │   ├── comment/
│   │   │   ├── CommentCard.tsx
│   │   │   ├── CommentForm.tsx
│   │   │   └── CommentList.tsx
│   │   ├── profile/
│   │   │   └── ProfileCard.tsx
│   │   └── common/
│   │       ├── TagList.tsx
│   │       └── ErrorAlert.tsx
│   ├── features/
│   │   ├── auth/
│   │   │   ├── api.ts           # 인증 API 호출
│   │   │   ├── hooks.ts         # 인증 훅 (TanStack Query)
│   │   │   ├── store.ts         # 인증 상태 (Zustand + persist)
│   │   │   ├── schemas.ts       # Zod 검증 스키마
│   │   │   └── types.ts         # 타입 정의
│   │   ├── article/
│   │   │   ├── api.ts
│   │   │   ├── hooks.ts         # TanStack Query 훅
│   │   │   ├── schemas.ts       # Zod 검증 스키마
│   │   │   └── types.ts
│   │   ├── comment/
│   │   │   ├── api.ts
│   │   │   ├── hooks.ts
│   │   │   └── types.ts
│   │   └── profile/
│   │       ├── api.ts
│   │       ├── hooks.ts
│   │       └── types.ts
│   ├── stores/
│   │   └── ui.ts                # UI 상태 (Zustand) - 모달, 사이드바 등
│   ├── routes/
│   │   ├── __root.tsx           # 루트 레이아웃
│   │   ├── index.tsx            # 홈 페이지
│   │   ├── login.tsx
│   │   ├── register.tsx
│   │   ├── settings.tsx
│   │   ├── editor/
│   │   │   ├── index.tsx        # 새 글 작성
│   │   │   └── $slug.tsx        # 글 수정
│   │   ├── article/
│   │   │   └── $slug.tsx        # 글 상세
│   │   └── profile/
│   │       ├── $username.tsx    # 프로필
│   │       └── $username.favorites.tsx
│   ├── lib/
│   │   ├── api.ts               # API 클라이언트 (ky)
│   │   ├── auth.ts              # 인증 유틸리티
│   │   └── utils.ts             # 공통 유틸리티
│   ├── test/
│   │   ├── setup.ts             # Vitest 셋업
│   │   └── test-utils.tsx       # 테스트 유틸리티 (MantineProvider wrapper)
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── public/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── Dockerfile
```

### 8.3 Infrastructure 구조

```
infra/
├── bin/
│   └── infra.ts                 # CDK 앱 진입점
├── lib/
│   ├── vpc-stack.ts             # VPC 스택
│   ├── database-stack.ts        # RDS 스택
│   ├── backend-stack.ts         # ECS 백엔드 스택
│   ├── frontend-stack.ts        # S3 + CloudFront 스택
│   └── pipeline-stack.ts        # CI/CD 파이프라인 (선택)
├── cdk.json
├── package.json
└── tsconfig.json
```

---

## 9. 개발 환경 설정

### 9.1 개발 환경 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           개발 환경 아키텍처                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────────┐   │
│  │   Local Dev     │     │    CI/CD        │     │    Production       │   │
│  ├─────────────────┤     ├─────────────────┤     ├─────────────────────┤   │
│  │ • Makefile      │     │ • GitHub        │     │ • AWS ECS Fargate   │   │
│  │ • Docker        │────▶│   Actions       │────▶│ • GitHub Pages      │   │
│  │ • SQLite        │     │ • Auto Test     │     │ • CloudWatch        │   │
│  │ • Vite HMR      │     │ • Auto Deploy   │     │ • X-Ray             │   │
│  └─────────────────┘     └─────────────────┘     └─────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 구성요소 | 기술 | 용도 |
|----------|------|------|
| 프로젝트 관리 | Makefile | 빌드, 테스트, 배포 명령어 통합 |
| 컨테이너화 | Docker | 일관된 개발/운영 환경 |
| CI/CD | GitHub Actions | 자동 테스트, 빌드, 배포 |
| 프론트엔드 배포 | GitHub Pages | 정적 사이트 호스팅 (무료) |
| 백엔드 배포 | AWS ECS + Fargate | 서버리스 컨테이너 |
| 인프라 | AWS CDK (TypeScript) | Infrastructure as Code |
| 모니터링 | CloudWatch + X-Ray | 로그, 메트릭, 분산 추적 |

### 9.2 필수 도구

```bash
# Backend
go version       # 1.21+
migrate -version # v4

# Frontend
node --version   # 20+
npm --version    # 10+

# Infrastructure
aws --version    # 2.x
cdk --version    # 2.x

# 공통
docker --version
docker-compose --version
make --version
```

### 9.3 로컬 개발 환경

```bash
# 저장소 클론
git clone https://github.com/your-org/realworld-vibe-coding.git
cd realworld-vibe-coding

# 환경 변수 설정
cp .env.example .env

# 의존성 설치
make install

# 데이터베이스 초기화 (SQLite)
make db-init

# 마이그레이션 실행
make migrate

# 개발 서버 실행
make dev
```

### 9.4 Makefile 전체 명령어

```makefile
.PHONY: help install dev test lint build deploy clean

# 도움말 표시
help:
	@echo "Available commands:"
	@echo "  make install     - Install all dependencies"
	@echo "  make dev         - Start development servers"
	@echo "  make test        - Run all tests"
	@echo "  make lint        - Run linters"
	@echo "  make build       - Build for production"
	@echo "  make deploy      - Deploy to production"
	@echo "  make clean       - Clean build artifacts"

# 의존성 설치
install:
	cd backend && go mod download
	cd frontend && npm ci
	cd infra && npm ci

# 개발 서버 실행 (Backend + Frontend 병렬)
dev:
	@./scripts/dev.sh

# 백엔드만 실행
dev-backend:
	cd backend && go run ./cmd/server

# 프론트엔드만 실행
dev-frontend:
	cd frontend && npm run dev

# 테스트 실행
test:
	cd backend && go test -v ./...
	cd frontend && npm run test

# 테스트 (watch 모드)
test-watch:
	cd frontend && npm run test:watch

# 테스트 커버리지
test-coverage:
	cd backend && go test -v -coverprofile=coverage.out ./...
	cd frontend && npm run test:coverage

# 린트 검사
lint:
	cd backend && golangci-lint run
	cd frontend && npm run lint

# 타입 체크
typecheck:
	cd frontend && npm run typecheck

# 프로덕션 빌드
build:
	cd backend && CGO_ENABLED=0 go build -o bin/server ./cmd/server
	cd frontend && npm run build

# Docker 이미지 빌드
docker-build:
	docker build -t conduit-backend ./backend
	docker build -t conduit-frontend ./frontend

# 데이터베이스 초기화 (SQLite)
db-init:
	cd backend && mkdir -p data && touch data/conduit.db

# 데이터베이스 시작 (PostgreSQL - Docker)
db-up:
	docker-compose up -d postgres

# 데이터베이스 중지
db-down:
	docker-compose down

# 마이그레이션 실행
migrate:
	cd backend && migrate -path db/migrations -database "$(DATABASE_URL)" up

# 마이그레이션 롤백
migrate-down:
	cd backend && migrate -path db/migrations -database "$(DATABASE_URL)" down 1

# 마이그레이션 상태 확인
migrate-status:
	cd backend && migrate -path db/migrations -database "$(DATABASE_URL)" version

# AWS CDK 배포
deploy:
	cd infra && npx cdk deploy --all

# CDK diff 확인
deploy-diff:
	cd infra && npx cdk diff

# GitHub Pages 배포 (프론트엔드)
deploy-frontend:
	cd frontend && npm run build
	cd frontend && npx gh-pages -d dist

# 빌드 아티팩트 정리
clean:
	cd backend && rm -rf bin/
	cd frontend && rm -rf dist/ node_modules/.vite/
	cd infra && rm -rf cdk.out/
```

### 9.5 Docker 설정

#### docker-compose.yml (로컬 개발용)

```yaml
version: '3.8'

services:
  # PostgreSQL (운영 환경 테스트용)
  postgres:
    image: postgres:16-alpine
    container_name: conduit-postgres
    environment:
      POSTGRES_USER: conduit
      POSTGRES_PASSWORD: conduit
      POSTGRES_DB: conduit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U conduit"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend (선택적)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: conduit-backend
    environment:
      - DATABASE_URL=postgres://conduit:conduit@postgres:5432/conduit?sslmode=disable
      - JWT_SECRET=dev-secret-key
      - SERVER_PORT=8080
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
```

#### Backend Dockerfile

```dockerfile
# backend/Dockerfile
FROM golang:1.21-alpine AS builder

WORKDIR /app

# 의존성 캐싱
COPY go.mod go.sum ./
RUN go mod download

# 소스 복사 및 빌드
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o server ./cmd/server

# 실행 이미지
FROM alpine:3.19

RUN apk --no-cache add ca-certificates tzdata

WORKDIR /app

COPY --from=builder /app/server .
COPY --from=builder /app/db/migrations ./db/migrations

EXPOSE 8080

CMD ["./server"]
```

#### Frontend Dockerfile

```dockerfile
# frontend/Dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

# 의존성 캐싱
COPY package*.json ./
RUN npm ci

# 소스 복사 및 빌드
COPY . .
RUN npm run build

# Nginx로 서빙
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 9.6 환경 변수

```bash
# .env.example

# ===================
# Database
# ===================
# 개발 환경 (SQLite)
DATABASE_URL=sqlite://./data/conduit.db

# 운영 환경 (PostgreSQL)
# DATABASE_URL=postgres://conduit:conduit@localhost:5432/conduit?sslmode=disable

# ===================
# JWT
# ===================
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRY=72h

# ===================
# Server
# ===================
SERVER_PORT=8080
SERVER_ENV=development  # development | staging | production

# ===================
# Frontend
# ===================
VITE_API_URL=http://localhost:8080/api

# ===================
# AWS (프로덕션)
# ===================
AWS_REGION=ap-northeast-2
AWS_ACCOUNT_ID=123456789012

# ===================
# Monitoring
# ===================
ENABLE_XRAY=false
LOG_LEVEL=debug  # debug | info | warn | error
```

### 9.7 모니터링 설정

#### CloudWatch 로그 그룹 구조

```
/conduit/
├── backend/
│   ├── application    # 애플리케이션 로그
│   ├── access         # HTTP 접근 로그
│   └── error          # 에러 로그
└── frontend/
    └── cloudfront     # CloudFront 접근 로그
```

#### X-Ray 트레이싱 설정

```go
// backend/internal/middleware/xray.go
package middleware

import (
    "github.com/aws/aws-xray-sdk-go/xray"
    "net/http"
)

func XRayMiddleware(next http.Handler) http.Handler {
    return xray.Handler(xray.NewFixedSegmentNamer("conduit-api"), next)
}
```

#### CloudWatch 메트릭 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│                    Conduit Dashboard                            │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Request Count   │  │ Error Rate      │  │ Latency P99     │ │
│  │     12.5K/min   │  │     0.02%       │  │     45ms        │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Request Latency (ms)                                    │   │
│  │ ▁▂▃▄▅▆▇█▇▆▅▄▃▂▁▂▃▄▅▆▇█▇▆▅▄▃▂▁                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Active Connections                                       │   │
│  │ ████████████░░░░░░░░░░░░░░░░░░  45/100                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 9.8 GitHub Pages 배포 설정

```yaml
# .github/workflows/deploy-frontend.yml
name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build
        run: |
          cd frontend
          npm run build
        env:
          VITE_API_URL: ${{ vars.API_URL }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

#### Vite 설정 (GitHub Pages용)

```typescript
// frontend/vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/realworld-vibe-coding/', // GitHub Pages 경로
  build: {
    outDir: 'dist',
    sourcemap: true,
  },
});
```

---

## 10. 테스트 전략

### 10.1 테스트 피라미드

```
                    ┌─────────┐
                   ╱           ╲
                  ╱   E2E (5%)  ╲
                 ╱               ╲
                ├─────────────────┤
               ╱                   ╲
              ╱  Integration (25%)  ╲
             ╱                       ╲
            ├─────────────────────────┤
           ╱                           ╲
          ╱       Unit Tests (70%)      ╲
         ╱                               ╲
        └─────────────────────────────────┘
```

### 10.2 Backend 테스트

```go
// 단위 테스트 예시: service/article_test.go
func TestArticleService_CreateArticle(t *testing.T) {
    // Given
    repo := NewMockArticleRepository()
    svc := NewArticleService(repo)

    req := CreateArticleRequest{
        Title:       "Test Article",
        Description: "Test Description",
        Body:        "Test Body",
        TagList:     []string{"test", "go"},
    }

    // When
    article, err := svc.CreateArticle(context.Background(), req, userID)

    // Then
    assert.NoError(t, err)
    assert.Equal(t, "test-article", article.Slug)
    assert.Equal(t, req.Title, article.Title)
}

// 통합 테스트 예시: api/handler/article_test.go
func TestCreateArticle_Integration(t *testing.T) {
    // Given
    db := setupTestDB(t)
    defer cleanupTestDB(t, db)

    router := setupRouter(db)
    token := createTestUserAndGetToken(t, db)

    body := `{"article":{"title":"Test","description":"Desc","body":"Body"}}`

    // When
    req := httptest.NewRequest("POST", "/api/articles", strings.NewReader(body))
    req.Header.Set("Authorization", "Token "+token)
    req.Header.Set("Content-Type", "application/json")

    rec := httptest.NewRecorder()
    router.ServeHTTP(rec, req)

    // Then
    assert.Equal(t, http.StatusCreated, rec.Code)
}
```

### 10.3 Frontend 테스트 (Vitest + React Testing Library)

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test/setup.ts',
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
  },
});
```

```typescript
// src/test/setup.ts
import '@testing-library/jest-dom/vitest';
import { vi } from 'vitest';

// Mock matchMedia for Mantine
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  })),
});
```

```typescript
// 단위 테스트 예시: components/ArticleCard.test.tsx
import { render, screen } from '@testing-library/react';
import { MantineProvider } from '@mantine/core';
import { describe, it, expect } from 'vitest';
import { ArticleCard } from './ArticleCard';

const renderWithMantine = (component: React.ReactNode) => {
  return render(
    <MantineProvider>
      {component}
    </MantineProvider>
  );
};

describe('ArticleCard', () => {
  it('renders article title and description', () => {
    const article = {
      slug: 'test-article',
      title: 'Test Article',
      description: 'Test Description',
      tagList: ['react', 'typescript'],
      createdAt: '2024-01-01T00:00:00Z',
      favorited: false,
      favoritesCount: 5,
      author: {
        username: 'johndoe',
        bio: '',
        image: '',
        following: false,
      },
    };

    renderWithMantine(<ArticleCard article={article} />);

    expect(screen.getByText('Test Article')).toBeInTheDocument();
    expect(screen.getByText('Test Description')).toBeInTheDocument();
  });
});

// 훅 테스트 예시: features/article/hooks.test.ts
import { renderHook, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { describe, it, expect, vi } from 'vitest';
import { useArticles } from './hooks';

const createTestQueryClient = () =>
  new QueryClient({
    defaultOptions: {
      queries: { retry: false },
    },
  });

describe('useArticles', () => {
  it('fetches articles successfully', async () => {
    const queryClient = createTestQueryClient();

    const { result } = renderHook(() => useArticles(), {
      wrapper: ({ children }) => (
        <QueryClientProvider client={queryClient}>
          {children}
        </QueryClientProvider>
      ),
    });

    await waitFor(() => {
      expect(result.current.isSuccess).toBe(true);
    });

    expect(result.current.data?.articles).toBeDefined();
  });
});
```

---

## 11. 배포 전략

### 11.1 환경 구성

| 환경 | 용도 | 트리거 |
|------|------|--------|
| Development | 로컬 개발 | 로컬 |
| Staging | 테스트/QA | PR 머지 → main |
| Production | 실제 서비스 | 수동 승인 후 배포 |

### 11.2 CI/CD 파이프라인

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Run tests
        run: |
          cd backend
          go test -v -race -coverprofile=coverage.out ./...
      - name: Upload coverage
        uses: codecov/codecov-action@v4

  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      - name: Lint
        run: |
          cd frontend
          npm run lint
      - name: Type check
        run: |
          cd frontend
          npm run typecheck
      - name: Test with Vitest
        run: |
          cd frontend
          npm run test:coverage
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: frontend/coverage/lcov.info

  deploy-staging:
    needs: [backend-test, frontend-test]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2
      - name: Deploy to staging
        run: |
          cd infra
          npm ci
          npx cdk deploy --all --require-approval never
```

### 11.3 인프라 스택 구성

```typescript
// infra/lib/backend-stack.ts
import * as cdk from 'aws-cdk-lib';
import * as ecs from 'aws-cdk-lib/aws-ecs';
import * as ecr from 'aws-cdk-lib/aws-ecr';

export class BackendStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props: BackendStackProps) {
    super(scope, id, props);

    // ECS Cluster
    const cluster = new ecs.Cluster(this, 'Cluster', {
      vpc: props.vpc,
    });

    // Fargate Service
    const service = new ecs.FargateService(this, 'Service', {
      cluster,
      taskDefinition: this.createTaskDefinition(),
      desiredCount: 2,
    });

    // Application Load Balancer
    const lb = new elbv2.ApplicationLoadBalancer(this, 'LB', {
      vpc: props.vpc,
      internetFacing: true,
    });

    const listener = lb.addListener('Listener', { port: 443 });
    listener.addTargets('Target', {
      port: 8080,
      targets: [service],
      healthCheck: {
        path: '/health',
      },
    });
  }
}
```

---

## 12. 구현 로드맵

### Phase 1: 프로젝트 셋업 (1주차)

- [x] 프로젝트 구조 생성
- [ ] 개발 환경 설정 (Docker, Makefile)
- [ ] CI/CD 파이프라인 구성
- [ ] 데이터베이스 스키마 및 마이그레이션
- [ ] sqlc 설정 및 기본 쿼리

### Phase 2: 인증 시스템 (2주차)

- [ ] 회원가입 API
- [ ] 로그인 API
- [ ] JWT 미들웨어
- [ ] 현재 사용자 조회/수정 API
- [ ] 로그인/회원가입 UI

### Phase 3: 아티클 CRUD (3주차)

- [ ] 아티클 생성 API
- [ ] 아티클 조회 (단일/목록) API
- [ ] 아티클 수정/삭제 API
- [ ] 아티클 에디터 UI
- [ ] 아티클 목록/상세 UI

### Phase 4: 댓글 및 태그 (4주차)

- [ ] 댓글 CRUD API
- [ ] 태그 목록 API
- [ ] 댓글 UI
- [ ] 태그 필터링 UI

### Phase 5: 프로필 및 팔로우 (5주차)

- [ ] 프로필 조회 API
- [ ] 팔로우/언팔로우 API
- [ ] 프로필 페이지 UI
- [ ] 설정 페이지 UI

### Phase 6: 피드 및 좋아요 (6주차)

- [ ] 피드 API
- [ ] 좋아요 API
- [ ] 홈 페이지 피드 탭 UI
- [ ] 좋아요 버튼 UI

### Phase 7: 최종 테스트 및 배포 (7주차)

- [ ] E2E 테스트
- [ ] 성능 테스트
- [ ] 보안 검토
- [ ] 프로덕션 배포
- [ ] 문서화 완료

---

## 13. 참고 자료

- [RealWorld Spec](https://realworld-docs.netlify.app/)
- [Agentic Coding - Armin Ronacher](https://lucumr.pocoo.org/2025/6/12/agentic-coding/)
- [Go Chi Router](https://github.com/go-chi/chi)
- [sqlc Documentation](https://sqlc.dev/)
- [Mantine UI](https://mantine.dev/)
- [TanStack Query](https://tanstack.com/query)
- [TanStack Router](https://tanstack.com/router)
- [AWS CDK](https://docs.aws.amazon.com/cdk/)
